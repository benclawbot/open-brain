__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.pytest-*/
.verification/

# Local secrets — never commit. Copy .env.example to .env and fill in your own
# values. `openbrain configure --project-root .` will generate a strong
# OPENBRAIN_API_KEY and DB_PASSWORD automatically on first run.
.env
